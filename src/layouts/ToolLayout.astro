---
interface Props {
  title: string;
  description?: string;
}

const { title, description = "無料で使えるWebツール" } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <title>{title} | Web Tools</title>
  </head>
  <body class="min-h-screen bg-gray-50 text-gray-900">
    <header class="bg-white shadow-sm">
      <nav class="mx-auto max-w-4xl px-4 py-4">
        <a href={base} class="text-xl font-bold text-indigo-600 hover:text-indigo-500 transition-colors">
          🛠 Web Tools
        </a>
      </nav>
    </header>

    <main class="mx-auto max-w-4xl px-4 py-8">
      <slot />
    </main>

    <footer class="mt-auto border-t border-gray-200 bg-white">
      <div class="mx-auto max-w-4xl px-4 py-6 text-center text-sm text-gray-500">
        &copy; {new Date().getFullYear()} Web Tools
      </div>
    </footer>
  </body>
</html>

<style is:global>
  @import "../styles/global.css";
</style>
