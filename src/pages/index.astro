---
import ToolLayout from "../layouts/ToolLayout.astro";

const tools = [
  {
    name: "ルーレット",
    description: "ランダムにアイテムを選ぶルーレット。罰ゲームや抽選に！",
    href: "/roulette/",
    emoji: "🎰",
  },
];
---

<ToolLayout title="無料Webツール集" description="すぐ使える便利な無料Webツール集">
  <div class="text-center mb-12">
    <h1 class="text-4xl font-bold mb-4">🛠 Web Tools</h1>
    <p class="text-lg text-gray-600">すぐ使える無料のWebツール集</p>
  </div>

  <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
    {tools.map((tool) => (
      <a
        href={tool.href}
        class="group block rounded-xl border border-gray-200 bg-white p-6 shadow-sm transition-all hover:shadow-md hover:border-indigo-300"
      >
        <div class="text-4xl mb-3">{tool.emoji}</div>
        <h2 class="text-xl font-semibold mb-2 group-hover:text-indigo-600 transition-colors">
          {tool.name}
        </h2>
        <p class="text-sm text-gray-500">{tool.description}</p>
      </a>
    ))}
  </div>
</ToolLayout>
